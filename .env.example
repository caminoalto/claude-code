# Git Configuration
# These will be automatically configured during container setup
GIT_USER_NAME="Your Name"
GIT_USER_EMAIL="your.email@example.com"

# Timezone Configuration
# Used by the devcontainer for system timezone
TZ="America/Los_Angeles"

# Network Security Configuration
# Configure which private network ranges are allowed for inbound/outbound connections

# Allow inbound connections from these private network ranges (comma-separated)
ALLOWED_INBOUND_RANGES="192.168.32.0/24,10.9.0.0/16"

# Allow outbound connections to these private network ranges (comma-separated)
ALLOWED_OUTBOUND_PRIVATE_RANGES="10.0.0.0/8,192.168.24.0/23"

# Additional DNS servers to allow (comma-separated)
# Note: DNS servers from /etc/resolv.conf are automatically allowed
DNS_SERVERS="192.168.16.1"

# Python Configuration
# Path to Python interpreter (optional override)
PYTHON_INTERPRETER_PATH="/usr/local/bin/python"